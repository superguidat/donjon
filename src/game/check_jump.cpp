#include	"donjon_master.hh"

/*
bool		ef::Unit::check_jump()
{
    double	now; // ou un équivalent SFML: sf::Clock::getElapsedTime().asSeconds()
    double	jump_cooldown; // 0.5s entre chaque saut

    now = glfwGetTime();
    jump_cooldown = 0.5;
    if (now - last_jump >= jump_cooldown && pos_cam.z < 0.1)
    {
        last_jump = now;
        pos_cam.z += 1.0; // impulse verticale du saut
        return true;
    }
    return false;
}
*/
