#include "donjon_master.hh"
/*
void    set_circle_points(t_coord &coo, double rayon)
{
  const int NB_POINTS = 30;
  const double step = (2.0 * M_PI) / NB_POINTS;

  double cx = coo.pos[0].x;
  double cy = coo.pos[0].y;
  double cz = coo.pos[0].z;

  for (int i = 0; i < NB_POINTS; ++i)
  {
    double angle = i * step;

    coo.pos[i] = {
      .x = cx + cos(angle) * rayon,
      .y = cy + sin(angle) * rayon,
      .z = cz
    };
  }
}
*/
